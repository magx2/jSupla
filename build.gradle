ext {
	slf4j_version = '1.7.25'
}

repositories {
	mavenLocal()
	mavenCentral()
}

allprojects {
	apply plugin: 'java'
	group = 'pl.grzeslowski.jSupla'
	version = '0.1.0-SNAPSHOT'
	sourceCompatibility = 1.8
}

// Add check style to all projects
allprojects { p ->
	if(!p.name.toLowerCase().contains("test")) { // only for non test projects
		apply plugin: 'checkstyle'
		p.tasks.withType(Checkstyle) {
			configFile = "${rootDir.path}/config/checkstyle/checkstyle.xml" as File
		}
	}
}

task wrapper(type: Wrapper) {
	gradleVersion = '4.0.1'
}