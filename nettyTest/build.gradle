repositories {
    maven { url 'https://repo.spring.io/milestone' }
    mavenCentral()
}

apply plugin: 'application'

dependencies {
	implementation rootProject.project("nettyServer")
	implementation group: 'org.slf4j', name: 'slf4j-simple', version: slf4j_version
}
