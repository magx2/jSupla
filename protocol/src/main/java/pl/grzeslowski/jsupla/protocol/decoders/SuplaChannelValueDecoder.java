package pl.grzeslowski.jsupla.protocol.decoders;

import pl.grzeslowski.jsupla.protocol.structs.SuplaChannelValue;

public class SuplaChannelValueDecoder implements Decoder<SuplaChannelValue> {
    @Override
    public SuplaChannelValue decode(byte[] bytes, int offset) {
        throw new UnsupportedOperationException();
    }
}
