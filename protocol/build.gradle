//noinspection GroovyAssignabilityCheck
jar {
    manifest {
        instruction "Export-Package", "pl.grzeslowski.jsupla.protocol.api*"
    }
}

dependencies {
    compile rootProject.project("common")
}
